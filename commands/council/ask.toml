description = "Consult the council. Usage: /council:ask [--json|--markdown] <query>"
prompt = """
The user has requested a Council Consultation.

<user_query>
{{args}}
</user_query>

Activate the 'council-chairman' skill and follow the **Consultation Protocol**. Note: Core project metadata is injected automatically by system hooks.
"""