description = "Check the council status"
prompt = """
The user wants to check the LLM Council status.

Activate the 'council-chairman' skill. Call `council/get_status` and report the current active models, reasoning effort, and whether the config is Project-specific or Global.
"""
